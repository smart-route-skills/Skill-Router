# Add mismatch aggregation over time to skill-router so repeated bad routes block promotion and suggest manifest trigger updates Builder

Duty: Owns implementation inside the validated ShipSpec contract.

## Notes

- Pending.

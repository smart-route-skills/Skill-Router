# Add full skill-router v2 behavior with LLM fallback, explicit selection modes, RoutedSkill output, JSONL route logs, strict manifest validation, and configurable mismatch thresholds Handoff

## Current State

- ShipSpec room created.

## Next Agent

- Planner confirms contract, then builder implements.

# Add optional OpenAI-backed fallback routing for ambiguous or no-match skill-router subtasks while keeping deterministic fake fallback for tests Builder

Duty: Owns implementation inside the validated ShipSpec contract.

## Notes

- Pending.
